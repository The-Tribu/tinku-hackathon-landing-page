---
import Diamond from "./Diamond.astro";

interface Props {
	text: string;
}

const { text } = Astro.props;
---

<div class="flex items-center gap-3 mb-4">
	<Diamond size="sm" />
	<span class="text-xs font-satoshi font-bold uppercase tracking-[0.2em] text-text-muted">
		{text}
	</span>
</div>
